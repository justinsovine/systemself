{
    "$schema": "https://json.schemastore.org/package.json",
    "name": "api",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "echo 'Laravel API build complete'",
        "dev": "./vendor/bin/sail up",
        "lint": "./vendor/bin/sail composer phpstan",
        "test": "./vendor/bin/sail artisan test",
        "migrate": "./vendor/bin/sail artisan migrate",
        "migrate:fresh": "./vendor/bin/sail artisan migrate:fresh --seed",
        "type-check": "echo 'Laravel type check complete'"
    }
}
